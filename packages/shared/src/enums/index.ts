/**
 * Domain enums live here, one file per module, e.g.:
 *   conference.enum.ts  -> ConferenceStatus, ReviewType
 *   paper.enum.ts        -> PaperStatus
 *   review.enum.ts        -> RecommendationType, AssignmentStatus
 *   decision.enum.ts      -> DecisionType
 *   audit.enum.ts          -> AuditAction
 *
 * Intentionally empty in Phase 0 (foundation scaffolding only) — enums are
 * added alongside the business module that owns them, in the phase that
 * implements it. Adding them here re-exports automatically once created.
 */
export {};