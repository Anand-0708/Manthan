/**
 * Shared request/response DTOs live here, one file per module, e.g.:
 *   paper.dto.ts
 *   review.dto.ts
 *   conference.dto.ts
 *
 * Intentionally empty in Phase 0 — populated alongside each business
 * module's API design as it's implemented.
 */
export {};